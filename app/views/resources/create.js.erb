$(function(){
  <%
    rt = Resource.get_resource_type(@resource.extension)
    content = render(partial:"campaigns/resource_row", locals: { resource: @resource, resource_type: rt.to_sym })
  %>
  $("#<%= rt %>-resources tbody").append("<%= escape_javascript content %>");
})
